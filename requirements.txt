# 数据下载/转换 + 长度 recon 需要；数据 pipeline 核心逻辑仅用标准库，离线自测无需安装。
datasets>=2.18.0
huggingface_hub>=0.23.0
pyarrow>=15.0.0
# 长度 recon(Qwen3 tokenizer + chat template)
transformers>=4.51.0
jinja2>=3.1.0
# socks 代理下载(国内)；非 socks 环境可不装
socksio>=1.0.0
